{
  "name": "sphere-2048",
  "version": "2.0.0",
  "description": "2048 game with Sphere wallet integration and Supabase backend",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix apps/web",
    "build": "npm run build --prefix apps/web",
    "typecheck": "npm run typecheck --prefix apps/web",
    "settle-and-pay": "npm run settle-and-pay --prefix apps/treasury-worker",
    "treasury:install": "npm install --prefix apps/treasury-worker"
  }
}
