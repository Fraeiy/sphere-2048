Continue the current 2048 game backend and replace the mock "batched transaction" logic with a real on-chain transaction on the Unicity network.

Context:
- There is a single treasury wallet (JSON key file already available)
- Users deposit into this wallet and their balances are tracked internally
- The game already batches moves (every 5 moves triggers a "transaction")
- Currently, the system only logs a fake tx hash and txId

Task:

1. When a user reaches 5 moves:
   - Generate a deterministic hash of the batched moves (use SHA256)
   - Prepare a real transaction payload using the treasury wallet

2. Create a valid Unicity transaction:
   - Sign the transaction using the treasury wallet JSON key
   - Include metadata (userId, number of moves, hash of moves)
   - Use correct Unicity transaction structure (UTXO or required format)

3. Broadcast the transaction to the Unicity network:
   - Connect to a Unicity node or RPC endpoint
   - Submit the signed transaction

4. Handle response:
   - Capture the real transaction hash returned by the network
   - Store it in the user session
   - Log it clearly in this format:
     [Chain] Real transaction submitted: user={userId}, txHash={hash}

5. Error handling:
   - Retry broadcast if it fails
   - Do not lose the batched moves if submission fails

6. Keep existing logic intact:
   - Do not break deposit system
   - Do not break internal balance tracking
   - Only replace the fake batching submission with real on-chain logic

Goal:
- Every 5 moves should now result in a real, verifiable on-chain transaction visible in the Unicity explorer